%li
  = image_tag(review.user.image, width: '54px', height: '54px', class: 'user_img pull-left')
  .user-profile.pull-left
    %span #{ review.user.name }
    %br
    %span #{ review.created_at.strftime('%Y年%m月%d日 %H:%M:%S') }
  .user-review
    #{review.body}
